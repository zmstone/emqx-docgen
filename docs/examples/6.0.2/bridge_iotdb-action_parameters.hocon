actions.iotdb.my_webhook_1.parameters {
  is_aligned = false
  # The device_id field is required if not provided in the MQTT message or EMQX SQL
  device_id = "template_value"
  data = [
    {
      #substruct(bridge_iotdb:action_parameters_data)
    }
  ]
  max_retries = 2
}