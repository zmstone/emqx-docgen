actions.alloydb.my_webhook_1.resource_opts {
  worker_pool_size = 16
  health_check_interval = "15s"
  health_check_interval_jitter = "0ms"
  # Health check timeout can be either a duration or infinity
  health_check_timeout = "60s"
  query_mode = async
  # Request TTL can be either a duration or infinity
  request_ttl = "45s"
  inflight_window = 100
  batch_size = 1
  batch_time = "0ms"
  max_buffer_bytes = "256MB"
}