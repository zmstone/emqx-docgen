actions.http.my_action {
  connector = "my_connector"
  tags = ["tag1"]
  parameters = {
    #substruct(bridge_http:parameters_opts)
  }
  resource_opts = {
    #substruct(bridge_http:action_resource_opts)
  }
}