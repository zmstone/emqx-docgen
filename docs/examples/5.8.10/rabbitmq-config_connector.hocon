connectors.rabbitmq.my_rabbitmq_1 {
  server = localhost
  port = 5672
  username = "my_user"
  password = "my_password"
  pool_size = 8
  timeout = "5s"
  virtual_host = "/"
  heartbeat = "30s"
  ssl = {
    #substruct(emqx:ssl_client_opts)
  }
  resource_opts = {
    #substruct(rabbitmq:connector_resource_opts)
  }
}