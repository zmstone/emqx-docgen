actions.redis.my_redis_action {
  local_topic = "t/1"
  parameters = {
    #substruct(bridge_redis:action_parameters)
  }
  connector = "my_redis_connector"
  tags = ["tag1", "tag2"]
  resource_opts = {
    #substruct(redis:action_resource_opts)
  }
}