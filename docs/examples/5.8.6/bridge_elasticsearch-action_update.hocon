actions.elasticsearch.my_action_name.parameters {
  action = update
  index = "${payload.index}"
  id = "${payload.id}"
  doc = "${payload.doc}"
  doc_as_upsert = false
  routing = "${payload.routing}"
  require_alias = false
  max_retries = 2
}