actions.s3.my_webhook_1.parameters {
  bucket = "example-bucket"
  key = "example-key"
  acl = "private"
  headers = {
    # Specify any key-value pairs for HTTP headers, e.g., "Content-Type" : "application/json"
  }
  mode = direct
  content = "${.}"
}