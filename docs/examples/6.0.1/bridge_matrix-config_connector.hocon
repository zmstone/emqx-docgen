connectors.matrix.my_webhook_1 = {
  server = "localhost:5432"
  disable_prepared_statements = false
  database = "example_db"
  pool_size = 8
  username = "username"
  password = "secret_password"
  ssl = {
    enable = false
  }
  resource_opts = {
    #substruct(connector_postgres:resource_opts)
  }
}