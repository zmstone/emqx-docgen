authorization.sources = [
  {
    type = mysql
    server = "127.0.0.1:3306"
    database = "authz_db"
    pool_size = 8
    username = "root"
    password = "your_password"
    ssl = {
      #substruct(emqx:ssl_client_opts)
    }
    prepare_statement = {
      # Map of SQL prepared statements
    }
    query = "SELECT * FROM authz_table WHERE user=?"
  }
]