authorization.sources = [
  {
    type = mongodb
    collection = "your_collection_name"
    filter = {}
    limit = 1000
    skip = 0
    mongo_type = sharded
    servers = "host1:27017,host2"
    w_mode = unsafe
    srv_record = false
    pool_size = 8
    username = "your_username"
    password = "your_password"
    use_legacy_protocol = auto
    auth_source = "auth_db_name"
    database = "db_name"
    topology = {
      #substruct(mongo:topology)
    }
    ssl = {enable = false}
  }
]