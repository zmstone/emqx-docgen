gateway.exproto = {
  server = {
    #substruct(gateway:exproto_grpc_server)
  }
  handler = {
    #substruct(gateway:exproto_grpc_handler)
  }
  mountpoint = ""  # Default value is an empty string
  listeners = {
    #substruct(gateway:tcp_udp_listeners)
  }
  enable_stats = true
  idle_timeout = "30s"
  clientinfo_override = {
    #substruct(gateway:clientinfo_override)
  }
}