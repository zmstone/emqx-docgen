connectors.s3.my_s3_transport.transport_options {
  ipv6_probe = false
  connect_timeout = "15s"
  pool_size = 8
  ssl = {
    #substruct(emqx:ssl_client_opts)
  }
  headers = {
    # Insert key-value pairs for HTTP headers here
  }
  max_retries = 0 # Example value, adjust as needed
  request_timeout = "30s" # Example value, adjust as needed
}