cluster.links = [
  {
    name = "remote_cluster_name"
    server = "mqtt.remote-emqx.com:1883"
    clientid = "base_client_id"
    username = "mqtt_user"
    password = "mqtt_password"
    ssl = {
      enable = false
    }
    topics = ["topic/1", "topic/2"]
    pool_size = 8
    retry_interval = "15s"
    max_inflight = 32
    resource_opts = {}
  }
]