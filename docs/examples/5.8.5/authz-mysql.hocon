authorization.sources = [
  {
    type = mysql
    server = "example.com:3306"
    database = "my_database"
    pool_size = 8
    username = "root"
    password = "your_password"
    ssl = {
      enable = false
    }
    prepare_statement = {
      "statement1" : "SELECT * FROM table WHERE id = ?"
    }
    query = "SELECT * FROM users WHERE username = ?"
  }
]