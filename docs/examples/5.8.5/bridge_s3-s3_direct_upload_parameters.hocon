actions.s3.my_webhook_1.parameters {
  bucket = "your-bucket-name"
  key = "your-object-key"
  acl = "private"
  headers = {
    # Specify your HTTP headers here, e.g., "Content-Type": "application/json"
  }
  mode = direct
  content = "${.}"
}