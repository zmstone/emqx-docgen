authentication = [
  {
    algorithm = public-key
    public_key = "-----BEGIN PUBLIC KEY-----"
    mechanism = jwt
    acl_claim_name = acl
    verify_claims = {}
    disconnect_after_expire = true
    from = password
    precondition = ""
  }
]