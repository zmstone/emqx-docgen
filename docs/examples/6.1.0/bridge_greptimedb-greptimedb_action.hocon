actions.greptimedb.my_webhook_1 {
  parameters = {
    #substruct(bridge_greptimedb:action_parameters)
  }
  fallback_actions = [
    #substruct(actions_and_sources:fallback_action_reference)
    #substruct(actions_and_sources:fallback_action_republish)
  ]
  connector = "my_connector"
  tags = ["tag1"]
  resource_opts = {
    #substruct(bridge_greptimedb:action_resource_opts)
  }
}