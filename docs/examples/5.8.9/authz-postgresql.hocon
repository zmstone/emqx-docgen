authorization.sources = [
  {
    type = postgresql
    server = "localhost:5432"
    disable_prepared_statements = false
    database = "my_database"
    pool_size = 8
    username = "my_user"
    password = "my_password"
    ssl = {
      #substruct(emqx:ssl_client_opts)
    }
    prepare_statement = {}
    query = "SELECT * FROM auth_data WHERE user_id = ?"
  }
]