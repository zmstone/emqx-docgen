authorization.sources = [
  {
    type = mysql
    server = "localhost:3306"
    database = "example_db"
    pool_size = 8
    username = "root"
    password = "your_secret_password"
    ssl = {
      #substruct(emqx:ssl_client_opts)
    }
    prepare_statement = {
      # Key-value pairs of SQL prepared statements
    }
    query = "SELECT * FROM authz_table WHERE user_id = ?"
  }
]