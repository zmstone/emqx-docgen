actions.s3.my_webhook_1.parameters {
  bucket = "your_s3_bucket_name"
  key = "your_s3_object_key"
  acl = "private"
  headers = {
    # Add key-value pairs for headers here, e.g., "Content-Type" = "application/json"
  }
  mode = direct
  content = "${.}"
}