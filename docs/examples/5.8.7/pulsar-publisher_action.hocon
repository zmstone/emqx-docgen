actions.pulsar.my_pulsar_action = {
  local_topic = "example_topic"
  parameters = {
    #substruct(pulsar:action_parameters)
  }
  connector = "my_connector"
  resource_opts = {
    #substruct(pulsar:action_resource_opts)
  }
}